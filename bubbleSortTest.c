#include <stdio.h>
void BUBBLE_SORT(int *T, int n)
{
    int flag = 1;
    while (flag)
    {
        flag = 0;
        for ()
    }
}
int main()
{
    int num;
    scanf("%d", &num);
    int test[num];
    for (int j = 0; j < num; j++)
    {
        scanf("%d", &test[j]);
    }
    BUBBLE_SORT(test);
    for (int i = 0; i < num; i++)
    {
        printf("%d", test[i]);
    }
    return 0;
}